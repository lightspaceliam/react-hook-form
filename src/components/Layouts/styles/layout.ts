import {
    makeStyles,
    createStyles,
} from '@material-ui/core/styles';

const styles = makeStyles(() =>
    createStyles({
		main: {
			paddingTop: '84px'
		},
  	}),
);

export default styles;