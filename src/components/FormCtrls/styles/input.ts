import {
    makeStyles,
    createStyles,
} from '@material-ui/core/styles';

const styles = makeStyles(() =>
    createStyles({
        root: {
            width: '100%',
        },
    }),
);

export default styles;